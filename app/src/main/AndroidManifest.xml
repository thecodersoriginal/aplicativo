<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.thecoders.tasktool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustNothing">
        <activity
            android:name=".Login"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Agenda" />
        <activity android:name=".MovimentoEstoque" />
        <activity
            android:name=".ServicoFuncionario"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Estoque" />
        <activity android:name=".OrdemDeServico" />
        <activity android:name=".TransferirServico" />
        <activity android:name=".AvaliarServico" />
    </application>

</manifest>